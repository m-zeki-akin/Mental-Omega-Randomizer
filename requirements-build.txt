PyInstaller==6.21.0
altgraph==0.17.5
packaging==26.2
pefile==2024.8.26
pyinstaller-hooks-contrib==2026.6
pywin32-ctypes==0.2.3
setuptools==83.0.0
websockets==17.0

# The launcher's shell. pywebview hosts the interface in the system's own
# WebView2, which Windows 10 and 11 ship; pythonnet is how it reaches it.
# The rest are what those two pull in.
pywebview==6.2.1
pythonnet==3.1.0
clr_loader==0.3.1
cffi==2.1.1
pycparser==3.0
bottle==0.13.4
proxy_tools==0.1.0
typing_extensions==4.16.0
